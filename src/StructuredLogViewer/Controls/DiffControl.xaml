<UserControl x:Class="StructuredLogViewer.Controls.DiffControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:diffplex="clr-namespace:DiffPlex.Wpf.Controls;assembly=DiffPlex.Wpf"
             xmlns:local="clr-namespace:StructuredLogViewer.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <UserControl.Resources>
        <DataTemplate x:Key="MyDataTemplate" DataType="{x:Type diffplex:DiffViewer}">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Stretch">
            </StackPanel>
        </DataTemplate>
    </UserControl.Resources>

    <Grid>
        <!-- <DataGrid
                x:Name="diffsView"
                ItemTemplate="{StaticResource MyDataTemplate}"
                ItemsSource="{Binding Path=DifferencesViewers, Mode=OneWay}">
        </DataGrid> -->
        <diffplex:DiffViewer x:Name="soloDiff"></diffplex:DiffViewer>
    </Grid>
</UserControl>
